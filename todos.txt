================================================================================
tailscale2otel — HANDOFF / CONTEXT / TODO  (for resuming with an LLM)
================================================================================
Last updated: 2026-06-02 (session 2). This file is the single source of truth for
picking up the project. Read it fully before changing code. The original design
plan lives at: /Users/rob/.claude/plans/we-are-building-a-cosmic-star.md
A detailed session log + learnings is in section 9 at the BOTTOM — read it.

--------------------------------------------------------------------------------
0. WHAT THIS PROJECT IS
--------------------------------------------------------------------------------
A lightweight Go service that polls the central Tailscale API
(api.tailscale.com/api/v2) for every available kind of observability data and
exports it as OTEL-native metrics + logs over OTLP. Optimized for Grafana Cloud
but works with any OTEL backend. Reference inspiration: ktranslate
(/Users/rob/repos/ktranslate) — sflow→OTEL; we apply the same pipeline shape to
the Tailscale API.

STATUS (after session 2): ALL P0 items are DONE and most of P1/P2. `go build ./...`,
`go vet ./...`, `gofmt -l .`, and `go test -race ./...` are all green across 22
packages (45 non-test .go files, 39 test files, ~200 test funcs). COMMITTED and
pushed-ready on `main` (commits 701ce13, 859ba93, bbcfc42 on top of 297d887/6c40c81).
VERIFIED END-TO-END against the REAL m7kni.io lab tailnet AND the real Grafana Cloud
OTLP endpoint: device/flow/audit/users/keys/settings/acl/dns metrics + flow & audit
LOGS all arrive in Mimir + Loki; self-obs (scrape/api/build_info/cache) present;
feature.enabled=1; DERP latency + routes emitted; flow throughput cross-checks
(~66 KB/s); /healthz=200; graceful exit 0. Real testing FOUND AND FIXED 4
production-breaking bugs (see section 9).
SESSION 3 (2026-06-03): ALL remaining actionable items DONE. P1-1 dashboards, P1-2
streaming auto_configure (PUT + wiring; live-capture pinning still deliberately
skipped), P1-4 cross-component dedup, P2-1 lint, P2-2 docs/metrics.md, P3 node-local
metrics scraper, P4 config.example.yaml, P5 flow nodeId->hostname all DONE and green
(go build/vet/gofmt/test -race ./... all pass; binary smoke-tested). P2-4 per-device
Resource is now a CLOSED DECISION: kept deferred (see P2-4 below for rationale). Only
P2-6 (blocked by pinned SDK) remains, by design. See section 10 for the session-3 log.

--------------------------------------------------------------------------------
1. CRITICAL CONTEXT FOR ANY LLM CONTINUING THIS
--------------------------------------------------------------------------------
- Module path: github.com/rknightion/tailscale2otel  (also the OTEL
  instrumentation scope name — keep them in sync).
- Go: go.mod declares `go 1.26.0`; toolchain go1.26.x. Use 1.26 features freely
  (e.g. testing/synctest, which is already used in internal/app/heartbeat_test.go).
- Build/test:
    go build ./...
    go vet ./...
    go test -race ./...
    gofmt -l .            # must be empty
- METHODOLOGY IS TDD (non-negotiable for this codebase): write a failing test
  first, run it, watch it fail for the right reason, write minimal code to pass,
  refactor green. Tests use ONLY stdlib `testing` (no testify).
- Assertions about emitted telemetry go through internal/telemetrytest.Recorder
  (an in-memory OTEL reader). Use it in every collector/processor test.
- Dependencies are pinned; do NOT casually run `go get`/`go mod tidy`. Current set:
    go.opentelemetry.io/otel v1.44.0 (+ /sdk, /sdk/metric, exporters otlpmetric{grpc,http}, stdout/stdoutmetric)
    go.opentelemetry.io/otel/log v0.6.0 (+ /sdk/log, exporters otlplog{grpc,http}, stdout/stdoutlog)  [GA but pre-1.0]
    github.com/tailscale/tailscale-client-go/v2 v2.0.0-20250129222324-74c8fc3cb4d7 (package `tsclient`)
    golang.org/x/oauth2, gopkg.in/yaml.v3, github.com/klauspost/compress (zstd)
- ARCHITECTURE PRINCIPLES: small single-purpose packages; collectors depend only
  on the frozen contracts (telemetry.Emitter, collector interfaces,
  enrich.DeviceCache, tsapi.Client, the flowlog/audit processors); each collector
  declares a NARROW client interface it needs (satisfied by *tsapi.Client) and
  fakes it in tests; the telemetry.Emitter facade is the ONLY thing that touches
  OTLP so OTLP details never leak into collectors.

--------------------------------------------------------------------------------
2. DESIGN DECISIONS ALREADY MADE (do not relitigate without reason)
--------------------------------------------------------------------------------
User decisions:
  - Language: Go (single static binary).
  - Flow logs modeled as BOTH aggregated metrics AND full-fidelity OTEL logs.
  - Export: OTLP push (gRPC/HTTP); stdout protocol for local debug.
  - Scope: ALL Tailscale data sources.
  - Webhook receiver: built, OFF by default.
  - Flow-log default log verbosity: per_connection (configurable: per_record | off).
  - Module/org: github.com/rknightion/tailscale2otel.
  - Both ingestion methods for flow+audit logs: POLL and STREAM (Splunk-HEC-style
    receiver). S3/GCS object-reader ingestion was DEFERRED (not built).
  - Target Go 1.26 and use its features where helpful.

Engineering decisions:
  - D1 Hybrid Tailscale client: official tsclient for snapshot resources; a thin
    custom HTTP doer (internal/tsapi/logs.go) for the two /logging endpoints the
    client does not expose. Both share one authenticated, retrying *http.Client.
  - D2 Use the official OTEL log SDK (NOT ktranslate's third-party logs lib).
  - D3 Flow-log "tail" hazard: never poll up to now; always poll [checkpoint,
    now-lag]. nextWindow() in internal/collector/window.go implements cold-start
    lookback, lag, and max-window capping.
  - D4 Device identity = METRIC ATTRIBUTES (host.name/host.id/os.type/...), not a
    per-device Resource. (Per-device Resource is a possible v2 change.)
  - D5 Synchronous instruments (Float64Counter.Add / Float64Gauge.Record), not
    ktranslate's async observable-gauge+channel design.
  - D7 Checkpoint store: in-memory (default) + file-backed (opt-in via
    checkpoint.store: file). Atomic file writes (temp+rename).
  - D8 Streaming ~= polling in content; the receiver emulates Splunk HEC
    (POST /services/collector/event, `Authorization: Splunk <token>`). Tailscale
    does NOT document the exact HEC payload envelope -> the parser is defensive
    and the envelope MUST be pinned empirically by capturing a live stream.
  - OTEL log SDK v0.6.0 has NO Record.EventName field; the event name is carried
    as the "event.name" attribute on each log record. SWITCH to the native field
    when the SDK adds it.
  - semconv-compliant naming: dotted lowercase, no `_total` suffix, UCUM units
    (By, {packet}, {flow}, {event}, s, d, 1). Low-cardinality dims on metrics;
    full 5-tuple / high-cardinality only on log records.

--------------------------------------------------------------------------------
3. HARD CONSTRAINTS DISCOVERED IN THE PINNED tailscale-client-go/v2
   (these forced deviations; verify with `go doc` before assuming otherwise)
--------------------------------------------------------------------------------
- tsclient.Device is FLAT and has NO ClientConnectivity field => NO DERP-latency
  data source in this version. `tailscale.device.derp.latency` is NOT emitted.
  (If the dep is upgraded to a Device with ClientConnectivity, add it back.)
- Device has NO "connectedToControl" flag => `tailscale.device.online` is derived
  from LastSeen recency within a window (default 5m) using an injectable clock
  (devices/export_test.go SetClock seam).
- Device field is `ID` (not `NodeID`); mapped to DeviceMeta.NodeID + host.id.
- Device has NO OSVersion; left "".
- Device timestamps (LastSeen/Created/Expires) are value type `tsclient.Time`
  (embeds time.Time); guard with .IsZero(), read .Unix().
- tsclient.Key has NO KeyType field => key "type" is derived from Capabilities
  (ephemeral|reusable|onetime). Key HAS Revoked (time.Time) and Invalid (bool).
- User.LastSeen and Key.Expires are plain time.Time (not the wrapper, not pointers).
- User.Role/Status/Type are defined string types (convert via string()).
- TailnetSettings bool fields that EXIST: DevicesApprovalOn, DevicesAutoUpdatesOn,
  UsersApprovalOn, NetworkFlowLoggingOn, RegionalRoutingOn,
  PostureIdentityCollectionOn (+ DevicesKeyDurationDays int). Fields that DO NOT
  exist in this version: HTTPSEnabled, ACLsExternallyManagedOn (omitted).
- RawACL has `HuJSON` (string) + `ETag` (string).
- DNSPreferences has `MagicDNS` (bool) only — NO OverrideLocal => the
  `tailscale.dns.override_local` metric is NOT emitted.
- telemetrytest.Recorder stringifies int64 log attributes as "" (Value.AsString
  on Int64 kind). int64 log attrs are still emitted correctly in production; tests
  assert their presence and verify values via the human-readable log Body. (A
  follow-up could improve the recorder to read int64 via Value.Emit/String.)

--------------------------------------------------------------------------------
4. PACKAGE INVENTORY (all under internal/ unless noted) + PUBLIC CONTRACTS
--------------------------------------------------------------------------------
cmd/tailscale2otel/main.go   - flags (-config), config.Load, slog setup, signal
                               handling, app.New, app.Run. version via -ldflags
                               "-X main.version=...".
internal/config              - Config + nested structs (yaml tags), Load(path),
                               Default(), (*Config).Validate(), Duration type
                               (UnmarshalYAML + .D()). ${ENV}/$ENV expansion via
                               os.Expand. Defaults-merge = Default() then Unmarshal
                               on top (explicit `enabled: false` DOES override).
internal/telemetry           - Emitter interface {Counter,Gauge,UpDownCounter,
                               LogEvent}; Attrs=map[string]any; Event{Name,Body,
                               Severity,Timestamp,Attrs}; Severity{Info,Warn,Error}.
                               NewEmitter(meter,logger) Emitter (otel-backed).
                               Provider: NewProvider(ctx, Options) (grpc|http|stdout
                               exporters, Resource, MeterProvider+PeriodicReader,
                               LoggerProvider+BatchProcessor); .Emitter(); .Shutdown().
                               Options has StdoutWriter seam for tests.
internal/telemetrytest        - Recorder.New(); .Emitter(); .MetricPoints(name)
                               []MetricPoint{Name,Unit,Kind,Value,Monotonic,Attrs};
                               .MetricNames(); .LogRecords() []LogRecord{Body,
                               SeverityText,EventName,Severity,Attrs}. TEST HELPER.
internal/semconv             - shared attribute keys, UCUM unit consts, enum values
                               (DirectionTransmit/Receive, TrafficVirtual/.../Physical,
                               NetworkTypeIPv4/IPv6). Collector-specific metric NAMES
                               live with each collector, not here.
internal/enrich              - DeviceCache: NewDeviceCache(opts...); Replace([]DeviceMeta);
                               ResolveName("addr:port")->hostname|"external"|"unknown";
                               LookupAddr/LookupNode; Len(); Age(); WithClock(). Knows
                               Tailscale ranges 100.64.0.0/10 + fd7a:115c:a1e0::/48.
internal/collector           - Collector/SnapshotCollector/WindowCollector interfaces;
                               Registry (Register / RegisterWindow / Entries);
                               CheckpointStore (NewMemoryStore / NewFileStore);
                               Scheduler (NewScheduler(emitter,store,opts...); Run(ctx,reg);
                               goroutine+ticker per collector, jitter, panic isolation,
                               window cursor via nextWindow + no-advance-on-error).
internal/flowlog             - record.go: NetworkResponse/FlowLog/ConnectionCounts.
                               processor.go: NewProcessor(cache, Options{LogMode,
                               IncludePorts,NodeDims}); Process(FlowLog,Emitter);
                               ProcessAll(NetworkResponse,Emitter). Consts MetricIO/
                               MetricPackets/MetricFlows. NOTE: NodeDims defaults
                               false in Go zero-value; the app sets it explicitly.
internal/audit               - record.go: ConfigurationResponse/Event/Actor/Target.
                               processor.go: NewProcessor(); Process(Event,Emitter);
                               ProcessAll(...). Const MetricAuditEvents.
internal/tsapi               - NewClient(Options); snapshot methods Devices/Users/Keys/
                               TailnetSettings/Webhooks/PolicyFileRaw/DNS*; log doers
                               NetworkFlowLogs(ctx,start,end) / ConfigAuditLogs(...).
                               auth.go (oauth|apikey), transport.go (retry 429/5xx +
                               Bearer auth), logs.go (custom GET + window query).
internal/collector/devices   - New(api lister, cache, interval). Feeds DeviceCache.
internal/collector/users     - New(api, interval).
internal/collector/keys      - New(api, interval, expiryWarn, now func()).
internal/collector/settings  - New(api, interval).
internal/collector/acl       - New(api, interval, now func()). Stateful (ETag).
internal/collector/dns       - New(api, interval).
internal/collector/flowlogs  - New(api, *flowlog.Processor, interval, lag). WindowCollector.
internal/collector/auditlogs - New(api, *audit.Processor, interval, lag). WindowCollector.
internal/stream              - New(Options{Listen,Path,Token,Decompress,TLSCertFile,
                               TLSKeyFile}, flowProc, auditProc, emitter, logger);
                               Handler() http.Handler; Run(ctx). HEC emulation,
                               gzip/zstd, defensive parse, shape-based routing.
internal/webhook             - New(Options{Listen,Path,Secret,Tolerance}, emitter,
                               logger); Handler(); Run(ctx). Verified Tailscale
                               HMAC scheme: header "t=<unix>,v1=<hex>", signed string
                               "<t>.<body>", HMAC-SHA256, constant-time compare.
internal/app                 - options.go (config->telemetry.Options incl. Grafana
                               Cloud Basic-auth header; config->tsapi.Options);
                               heartbeat.go (runHeartbeat -> tailscale2otel.up);
                               app.go (New/Run, provider+client+cache+scheduler,
                               graceful shutdown); collectors.go (registerCollectors
                               source-gated; buildReceivers).

Packaging: deploy/Dockerfile (multi-stage, distroless nonroot), config.example.yaml,
README.md, .dockerignore, .gitignore.

--------------------------------------------------------------------------------
5. AS-BUILT TELEMETRY CATALOG (what is emitted today)
--------------------------------------------------------------------------------
METRICS (name [unit] — key attributes):
  tailscale2otel.up [1]                        - heartbeat (self-obs).
  tailscale.network.io [By]                    - network.io.direction, network.transport,
                                                 tailscale.traffic_type, tailscale.src.node,
                                                 tailscale.dst.node (+ ports if IncludePorts).
  tailscale.network.packets [{packet}]         - same dims.
  tailscale.network.flows [{flow}]             - network.transport, tailscale.traffic_type.
  tailscale.config.audit.events [{event}]      - tailscale.audit.action, tailscale.audit.origin.
  tailscale.device.online [1]                  - host.name, host.id, os.type, tailscale.user.
  tailscale.device.last_seen [s]               - id dims.
  tailscale.device.key.expiry [s]              - id dims.
  tailscale.device.update_available [1]        - id dims.
  tailscale.devices.count [1]                  - os.type, tailscale.authorized, tailscale.external.
  tailscale.users.count [1]                    - tailscale.user.role/status/type.
  tailscale.user.devices [1]                   - enduser.id, tailscale.user.login.
  tailscale.user.connected [1]                 - id dims.
  tailscale.user.last_seen [s]                 - id dims.
  tailscale.key.expiry [s]                     - tailscale.key.id/type/description.
  tailscale.keys.count [1]                     - tailscale.key.type/revoked/invalid.
  tailscale.setting.enabled [1]                - tailscale.setting.name (6 bools).
  tailscale.setting.devices_key_duration [d]   - (no attrs).
  tailscale.acl.last_changed [s]               - (no attrs; on ETag change).
  tailscale.acl.size [By]                      - raw HuJSON length.
  tailscale.dns.nameservers.count [1]          -
  tailscale.dns.search_paths.count [1]         -
  tailscale.dns.split_zones.count [1]          -
  tailscale.dns.magic_dns [1]                  -
  tailscale.stream.records [Counter]           - type=flow|audit.
  tailscale.stream.rejected [Counter]          - reason=auth|unparsable.
  tailscale.webhook.events [Counter]           - tailscale.webhook.type.
  tailscale.webhook.rejected [Counter]         - reason=...

LOG RECORDS (event.name — when):
  tailscale.network.flow      - per flow connection (LogMode per_connection) or per
                                record (per_record); off disables. Carries 5-tuple,
                                src/dst node, tx/rx bytes+packets, traffic_type.
  tailscale.config.audit      - per audit event; WARN if event has an error.
  tailscale.key.expiring      - WARN when a key expires within expiry_warn.
  tailscale.webhook.<type>    - per webhook event; WARN for Expir/Suspend/NeedsApproval/Deleted.

--------------------------------------------------------------------------------
6. COMPLETED (milestones from the plan)
--------------------------------------------------------------------------------
[x] M0 skeleton + OTLP heartbeat (smoke-verified end to end)
[x] M1 Tailscale client + auth (OAuth + API key, retry transport)
[x] M2 collector/scheduler framework (registry, scheduler, checkpoints, window math)
[x] M3 devices collector + enrichment cache (cache feeder)
[x] M4 flow logs POLL + shared flowlog.Processor + checkpoint + aggregation
[x] M5 audit logs POLL + shared audit.Processor
[x] M6 snapshot collectors: users, keys, settings, acl, dns
[x] M7 HTTP receivers: Splunk-HEC stream + HMAC webhook (off by default)
[x] M8 packaging: Dockerfile, config.example.yaml, README, .dockerignore, .gitignore
[x] self-observability: scrape.*, api.requests/retries, export.failures, build_info,
    enrich.cache_age/size — all done in session 2 (P0-1).
[x] real-tailnet + Grafana Cloud end-to-end verification (P0-4) — session 2.
[x] CI/CD: .github/workflows/{ci,release}.yml (P0-5) — session 2.
[x] Helm chart (deploy/helm/tailscale2otel) + docker-compose (deploy/docker-compose.yaml).

--------------------------------------------------------------------------------
7. PENDING / TODO  (prioritized; each item is actionable for an LLM)
   P0 = needed for production; P1 = important for feature-complete; P2 = nice-to-have
--------------------------------------------------------------------------------
P0-1  [DONE session 2] SELF-OBSERVABILITY scrape metrics. Enhance
      internal/collector/scheduler.go runTick/runWindow to emit, per collector
      (attr tailscale.collector=<name>): tailscale2otel.scrape.duration [s] (or a
      histogram), tailscale2otel.scrape.success [1] (1/0 last run),
      tailscale2otel.scrape.errors [Counter] {error.type}, .scrape.last_timestamp [s].
      Also surface tsapi request/retry counters (tailscale2otel.api.requests
      {http.response.status_code,endpoint}, .api.retries) — likely via an Emitter
      hook in tsapi/transport.go. And OTLP export failures via otel.SetErrorHandler
      -> tailscale2otel.export.failures. Gate on cfg.SelfObservability.Enabled. TDD
      with telemetrytest. Pass the Emitter into the scheduler (already has it).

P0-2  [DONE session 2] CONFIG VALUES wired (note: collect_routes is now INLINE via
      DevicesRich ?fields=all — NO per-device fan-out; cache_ttl emits cache_age/size
      but the optional age>ttl WARN was not added). Original notes:
      - enrichment.cache_ttl: nothing reads it. Emit tailscale2otel.enrich.cache_age
        [s] (DeviceCache.Age()) + cache_size (Len()) from the devices collector or a
        self-obs hook; optionally WARN when age>cache_ttl.
      - cardinality.collapse_external: defined but NOT plumbed into flowlog.Processor
        (Options lacks it; ResolveName always collapses). Either add the option +
        honor it, or drop the config field. (flow_include_ports + flow_node_dims ARE
        wired via app.flowOptions.)
      - collectors.devices.collect_routes / collect_posture: defined but unused. Wire
        them to call tsapi SubnetRoutes / posture (add tsapi methods) and emit
        tailscale.device.routes.advertised/.enabled [{route}] and posture attrs.
        These are per-device fan-out (N API calls); keep default false.

P0-3  [DONE session 2] WINDOW BOUNDARY DEDUP (in both window collectors, via
      internal/dedup bounded set; keys exactly as below). flowlogs/auditlogs return `to`
      and next window starts at `to` (inclusive ranges) -> a boundary-timestamp
      record can be emitted twice. Implement dedup-by-key (flows:
      nodeId|start|end|proto|src|dst; audit: eventGroupID|eventTime) in the
      collectors, or track lastSeen keys. Currently only mitigated by the lag window.

P0-4  [DONE session 2 — fully verified in Grafana Cloud; feature self-disable
      IMPLEMENTED via flowlogs.FeatureCheck closure reading NetworkFlowLoggingOn +
      403 handling]. REAL-TAILNET END-TO-END. Run against a real tailnet with OAuth
      client and a real Grafana Cloud OTLP endpoint. Verify: device/flow/audit
      metrics+logs arrive; cross-check flow throughput vs the Tailscale admin
      console; confirm graceful self-disable when networkFlowLoggingOn=false or on
      403 (flow logs require Premium/Enterprise + the feature enabled). NOTE: the
      "feature disabled -> idle + emit tailscale.feature.enabled=0" behavior from the
      plan (§8) is NOT implemented yet — add it to the flowlogs collector (check
      TailnetSettings.NetworkFlowLoggingOn or handle 403 gracefully).

P0-5  [DONE session 2 — .github/workflows/ci.yml + release.yml]. CI: .github/workflows
      for `go build`, `go vet`, `gofmt -l` (fail if
      non-empty), `go test -race ./...`, and `govulncheck`. Add a release workflow
      (goreleaser or docker buildx) building the image from deploy/Dockerfile.

P1-1  [DONE session 3 — deploy/grafana/{tailscale-fleet,tailscale-network,tailscale-audit-events,
      tailscale-exporter-health}.json (Grafana 13, DS_PROM/DS_LOKI vars, dynamic multi-value
      variables) + deploy/grafana/README.md + README references]. Original notes:
      now buildable against live data (gcx context
      `m7kni`, default prom ds `grafanacloud-prom`, loki `grafanacloud-logs`; metric
      names are OTLP-normalized, e.g. tailscale.network.io -> tailscale_network_io_bytes_total,
      unit-1 gauges get a _ratio suffix e.g. tailscale_device_online_ratio).
      GRAFANA DASHBOARD(S) in deploy/grafana/*.json: device fleet (online/last-seen/
      update-available/count), flow throughput by node/transport/traffic_type
      (tailscale.network.io rate), audit event stream (logs), key-expiry table,
      and exporter self-health (tailscale2otel.up + scrape metrics). Can be built
      with the gcx create-dashboard tooling. Update README to reference it. Make heavy
      use of Grafana 13's Dynamic Dashboards functionality.

P1-2  [DONE session 3 — auto_configure PUT implemented (tsapi.ConfigureLogStream +
      putJSON; retry transport now rewinds the body via GetBody) and wired in app
      (App.autoConfigureStreaming, gated by streaming.auto_configure, requires
      streaming.enabled + public_url, validated). LIVE-CAPTURE pinning STILL skipped by
      design — never reconfigures the lab tailnet's streaming]. Original notes:
      stream parser hardened + realistic fixtures (numeric proto,
      polymorphic audit) added; LIVE-CAPTURE pinning intentionally SKIPPED (must NOT touch
      the lab tailnet's streaming config). STILL TODO: streaming.auto_configure PUT
      /tailnet/{tailnet}/logging/{type}/stream (off by default; add tsapi method + app wiring).
      STREAMING ENVELOPE PINNING. The HEC parser in internal/stream is defensive
      against an UNDOCUMENTED Tailscale payload. Configure a real tailnet to stream
      to the receiver (private endpoint over the tailnet, HTTPS via `tailscale cert`),
      capture an actual POST body, add it as a fixture, and tighten the parser.
      Also: streaming.auto_configure (PUT /tailnet/{tailnet}/logging/{type}/stream to
      self-register the receiver as a sink; needs log_streaming scope) is NOT
      implemented — add it (off by default). You must not change the streaming of the 
      existing tailnet used for testing

P1-3  [DONE session 2] MISSING DATA SOURCES — user-invites (tailscale.user_invites.count),
      device routes (inline via DevicesRich), acl.rules (hujson-parsed) all implemented.
      Original notes:
      - user-invites: tailscale.user_invites.count (add tsapi method + emit in users).
      - device routes (per-device): see P0-2.
      - acl.rules per-section counts: parse RawACL.HuJSON with github.com/tailscale/hujson
        and emit tailscale.acl.rules {tailscale.acl.section}. (Currently only acl.size.)

P1-4  [DONE session 3 — optional shared dedup.Set on BOTH processors: audit (WithDedup +
      exported DedupKey) and flowlog (Options.Dedup, FlowLog-window keyed; flow records are
      immutable once their window closes so window-granularity dedup is safe). App wires one
      shared set per processor (flow cap 16384, audit cap 4096) so poll + stream don't
      double-count. Webhook<->audit cross-dedup is best-effort/not covered: webhook events do
      not flow through audit.Processor and may lack eventGroupID]. NOTE: poll-window boundary dedup IS done (P0-3, per-collector).
      What remains is CROSS-component dedup. Because the app shares ONE flowProc/auditProc
      between the poll collectors AND the stream receiver, the cleanest fix is an optional
      shared dedup.Set on the PROCESSORS (keyed by eventGroupID|eventTime for audit). Webhook
      payloads may NOT carry eventGroupID, so webhook<->audit cross-dedup is best-effort.
      STREAM/WEBHOOK <-> AUDIT DEDUP. Webhook events overlap with audit-log events;
      dedupe by eventGroupID (LRU set) so enabling both webhooks and auditlogs does
      not double-count. (Plan §3j.)

P1-5  [DONE session 2 — internal/app/app_test.go uses a newApp() seam with a recorder
      emitter + stub client; asserts Run()+graceful shutdown returns nil and emits
      heartbeat + build_info]. app-level integration test: build App with a recorder seam and
      assert New()+Run(short ctx)+graceful Shutdown returns cleanly and emits the
      heartbeat. (Currently only options + heartbeat are unit-tested; Run() is only
      smoke-tested manually.) Consider adding a telemetry.Provider test seam or an
      app.New variant that accepts a pre-built Emitter for assertion.

P1-6  [DONE session 2 — global stdlib token-bucket rate limiter as the OUTERMOST tsapi
      transport; config tailscale.http.rate_limit (req/s, 0=unlimited). Validated at 15 req/s
      against the 89-device lab tailnet with no issues]. RATE LIMITS & PAGINATION. Tailscale API
      has no pagination (full result sets) and undocumented rate limits; the retry
      transport honors Retry-After. Validate under a large tailnet; consider a global
      rate limiter across collectors.

P2-1  [DONE session 3 — config/validate.go oneOf now uses slices.Contains; telemetrytest
      recorder metric-attr rendering uses attribute.Value.String() (the non-deprecated
      replacement for the deprecated .Emit(); identical output for our scalar attrs); also
      maps.Copy in flowlog dirAttrs and strings.CutSuffix in the nodemetrics parser]. Earlier:
      DONE: maps.Copy (app/options.go), strings.SplitSeq (stream+webhook),
      removed dead logOff const. REMAINING (minor): slices.Contains in
      internal/config/validate.go + telemetrytest test; deprecated kv.Value.Emit() in
      internal/telemetrytest/recorder.go (use .String()) — NOTE recorder is a SHARED test
      dep imported by ~everything, so change it ALONE (not concurrently with a swarm) and
      run the FULL `go test -race ./...` to catch attr-rendering regressions.
P2-2  [DONE session 3 — docs/metrics.md authored: naming conventions + OTLP->Prom
      normalization rules, full metrics table, log-events table, node-metrics scraper
      section, cross-source dedup note, and Grafana PromQL/LogQL query examples]. Helm
      chart + docker-compose were already DONE in session 2.
P2-3  [DONE session 2] build_info metric (tailscale2otel.build_info {service.version,go.version})
      + always-on admin server (config `admin: {enabled,listen}`) serving /healthz + /readyz.
P2-4  [DEFERRED — CLOSED DECISION, session 3 (user-confirmed)]. Per-device Resource
      (D4 alternative) will NOT be implemented. Rationale: OTEL metrics have exactly one
      Resource per MeterProvider, so true per-device Resource requires N per-device
      MeterProviders/readers (86 on the lab tailnet) plus a target_info series per device =
      churn/cost on Grafana Cloud, for ~zero new capability — device identity is ALREADY on
      every metric as attributes (host.name/host.id/os.type/os.version/tailscale.user via
      D4), which already supports per-device filter/group/alert. The user separately wanted
      node-level metrics in OTEL; that is delivered by P3 (node-metrics scraper, label-based
      "instance" identity), NOT by per-device Resource. If a host-centric Entity-Graph view is
      ever needed, revisit then. (Was: significant data-model change, gated off by default.)
P2-6  [BLOCKED — not actionable] pinned OTEL log SDK v0.6.0 has NO Record.EventName field;
      we carry it as the "event.name" attribute. Revisit only if the SDK is upgraded.

P3  [DONE session 3 — internal/collector/nodemetrics: gated SnapshotCollector that scrapes
      a CONFIGURED list of Prometheus-text /metrics endpoints (config collectors.node_metrics
      {enabled,interval,timeout,targets:[{url,instance,labels}]}) and re-emits VERBATIM names
      via the Emitter — counters (and histogram/summary _bucket/_sum/_count) as DELTAS with
      reset handling, gauges as gauges, plus a per-target tailscale.node.up gauge. Node
      identity is the "instance" LABEL (user-confirmed: NOT per-node Resource), single
      MeterProvider. Stdlib-only Prometheus text parser. Off by default; disabled with no
      targets. App registers it when enabled && len(targets)>0]. NOTE: the lab tailnet ALREADY exposes tailscaled_*
      metrics in Grafana (some existing scraper), so there is real precedent/value.
      Design: a new gated collector (internal/collector/nodemetrics) that scrapes a
      CONFIGURED list of Prometheus /metrics endpoints (no tailnet auto-discovery from
      this exporter) and re-emits via the Emitter. CAREFUL with counter semantics: scraped
      cumulative counters must be forwarded as DELTAS (track prev value per series, handle
      resets) via Emitter.Counter, gauges as Gauge. Stdlib-only Prometheus text parser
      (no new dep). Gate off by default (no targets = disabled).
    Node-local client metrics (tailscaled per-node /metrics) as an optional richer
      data source (different architecture: per-node scrape, not central API). Tailscale
      has this today subject to it being enabled on the tailnet and ACLs set up (research first)
      and our collector can act as a prometheus scraper then transform to OTEL and send all
      the tailscale node metrics centrally. Match the configured default scrape interval.
      I have disabled scraping on your approved test environment so you can avoid seeing all the old data.

P4  [DONE session 2 + session 3] Default OTLP push interval changed 30s -> 60s
    (config/defaults.go). config.example.yaml UPDATED in session 3: metric_interval 30s->60s,
    added tailscale.http.rate_limit, admin.{enabled,listen}, streaming.public_url, and the
    collectors.node_metrics section; a new config test loads+validates the example to prevent
    drift. Collector intervals
    already >=60s. Aligns push cadence with scrape to avoid Grafana Cloud DPM churn.

P5. [DONE session 3 — flowlog.Processor now adds tailscale.node.hostname to flow log records
    (both per_connection and per_record) via cache.LookupNode(flow.NodeID), only when the
    cache resolves a non-empty hostname]. Enrich the flow-log nodeId with hostname from the device cache.
    NOTE: the cache's byNode index is NOW keyed by the nDdi... NodeID (DevicesRich.NodeID),
    which MATCHES flow.NodeID, so LookupNode(flow.NodeID) will hit. Add the hostname as an
    attribute on the flow log/metric in flowlog.Processor (it currently passes flow.NodeID raw
    as tailscale.node.id). The DeviceCache would need a LookupNode-by-flow-nodeId helper or
    the processor calls cache.LookupNode(flow.NodeID).Hostname.

--------------------------------------------------------------------------------
8. HOW TO CONTINUE (workflow for the LLM)
--------------------------------------------------------------------------------
1. Read this file + the plan at /Users/rob/.claude/plans/we-are-building-a-cosmic-star.md.
2. Pick a TODO. For anything touching telemetry, FIRST read the frozen contracts:
   internal/telemetry/types.go, internal/collector/collector.go,
   internal/enrich/devicecache.go, internal/telemetrytest/recorder.go,
   internal/semconv/attrs.go, and the relevant processor.
3. Confirm any tsclient field/method via `go doc github.com/tailscale/tailscale-client-go/v2.<Type>`
   before using it (the pinned version differs from intuition — see section 3).
4. TDD: failing test -> watch fail -> minimal impl -> green -> refactor.
5. Keep packages small + single-purpose; collectors take a narrow client interface +
   fake it in tests; assert via telemetrytest.Recorder.
6. After each unit: `go build ./... && go vet ./... && gofmt -l . && go test -race ./...`.
7. Independent units should be parallelized across subagents (each owns one package,
   no go.mod edits). Add new dependencies up front so agents   don't contend on go.mod/go.sum.
8. You can commit to main and push at any time. This project is new and not being used yet.

--------------------------------------------------------------------------------
9. SESSION LOG + LEARNINGS  (session 2, 2026-06-02 — READ THIS BEFORE CONTINUING)
--------------------------------------------------------------------------------
WHAT WAS DONE: All P0 + P1-3/P1-5/P1-6 + P2-2(partial)/P2-3 + P4, built by a phased
parallel-workflow swarm (each agent owned ONE package; strict TDD), then app-layer
integration done by hand. Commits on main: 701ce13 (Phase 1 foundations + bug fixes
+ CI), 859ba93 (Phase 2+3 self-obs/rich-devices/dedup/app-wiring), bbcfc42 (OTLP 404 fix).

REAL-TAILNET TESTING (the lab is m7kni.io; read-only creds + Grafana Cloud OTLP creds
are in the GITIGNORED .secrets/creds.local.env — `set -a; source .secrets/creds.local.env;
set +a`. NEVER commit .secrets/, .capture/, or config.{local,smoke,lowlog}.yaml — all
gitignored. Real API captures are in .capture/ for fixtures). Tailnet has 89 devices,
flow logging ON, posture collection ON.

4 PRODUCTION-BREAKING BUGS found ONLY by running against real data (would never have been
caught by the unit tests against synthetic fixtures):
  1. flow-log `proto` is a NUMBER on the wire (1/6/17/99/absent), not a string -> the old
     `Proto string` made json.Decode FAIL outright -> flow polling silently produced NOTHING.
     Fixed: ConnectionCounts.Proto int + transportName() IANA-number->name mapper.
  2. audit `old`/`new` are POLYMORPHIC (string|object|array|null) -> old `string` type failed
     to decode object/array values (ACL diffs, {"PostureDisabled":true}). Fixed: json.RawMessage.
  3. OTLP/HTTP to Grafana Cloud 404'd: the OTEL otlphttp WithEndpointURL uses the URL path
     AS-IS and does NOT append /v1/metrics, so a base ".../otlp" endpoint 404s. Fixed:
     otlpHTTPURL() appends /v1/{metrics,logs} (telemetry/provider.go).
  4. graceful SIGINT returned context.Canceled -> process exited non-zero. Fixed: app.Run
     treats any ctx error from the scheduler as a clean stop (returns nil).

KEY ARCHITECTURAL WIN: the raw REST `GET /devices?fields=all` returns FAR richer data than
the pinned tsclient.Device — real `connectedToControl` online flag, per-DERP-region latency
(the previously-"unavailable" DERP source — NOW EMITTED as tailscale.device.derp.latency),
`advertisedRoutes`/`enabledRoutes` INLINE (so collect_routes needs NO per-device fan-out),
`distro{name,version}` (os.version), and `nodeId` (nDdi form, used to key the cache byNode).
tsapi.DevicesRich() wraps this (hybrid-doer pattern like tsapi/logs.go). The devices
collector now uses it instead of tsclient.Devices().List.

GRAFANA CLOUD VERIFICATION (gcx CLI, context `m7kni`, Grafana 13.1.0):
  - Metrics land in Mimir (default prom ds uid `grafanacloud-prom`). OTLP->Prom normalization:
    dots->underscores, counters get `_total`, units get suffixes (By->_bytes, s->_seconds,
    d->_days), and UNIT "1" gauges get a `_ratio` suffix (e.g. tailscale_device_online_ratio,
    tailscale2otel_up_ratio). Account for this when writing dashboards/alerts.
  - Logs land in Loki (ds uid `grafanacloud-logs`, label service_name="tailscale2otel").
  - gcx GOTCHA: `gcx logs query` REQUIRES BOTH --from AND --to (it silently errors otherwise).
    The CLI was flaky for me; Grafana Explore is the source of truth. Useful PromQL checks:
    `tailscale2otel_up_ratio`, `count(tailscale_device_online_ratio==1)`,
    `sum(rate(tailscale_network_io_bytes_total[2m]))` (~66 KB/s observed).

NEW METRICS added this session (semconv units in []):
  tailscale2otel.scrape.{duration[s],success[1],errors[1]{error.type},last_timestamp[s]}{tailscale.collector}
  tailscale2otel.api.{requests[1]{endpoint,http.response.status_code},retries[1]{endpoint}}
  tailscale2otel.export.failures[1]{error.type}, tailscale2otel.build_info[1]{service.version,go.version}
  tailscale2otel.enrich.{cache_age[s],cache_size[1]}
  tailscale.device.derp.latency[s]{host.name,host.id,tailscale.derp.region,tailscale.derp.preferred}
  tailscale.device.routes.{advertised,enabled}[{route}]{host.name,host.id}  (gated collect_routes)
  tailscale.feature.enabled[1]{tailscale.feature}, tailscale.user_invites.count[1]{role,accepted}
  tailscale.acl.rules[1]{tailscale.acl.section}
  NEW log event: tailscale.device.posture (gated collect_posture).

NEW CONFIG: tailscale.http.rate_limit (req/s, 0=off); admin.{enabled,listen} (/healthz);
  default otlp.metric_interval now 60s. config.example.yaml NOT yet updated for these — DO in
  session 3 (it still shows metric_interval 30s and lacks rate_limit/admin keys).

ORCHESTRATION NOTES for session 3 (this was an "ultracode" swarm session):
  - Phase by the IMPORT GRAPH. Shared-dep packages (internal/telemetry, internal/collector
    top-level, internal/telemetrytest, internal/config, internal/semconv) are imported by many
    leaves; editing them CONCURRENTLY with leaf agents causes transient compile races. Sequence
    shared-dep changes BEFORE the leaf fan-out (or do them inline), then fan out the leaves.
  - Type changes ripple to CONSUMER test fixtures in OTHER packages the agent doesn't own
    (e.g. Proto int broke collector/flowlogs + stream test fixtures). Fix consumers between phases.
  - Always verify the WHOLE module (go build/vet/gofmt/test -race) + commit a GREEN state
    between phases. Leaf agents passing in isolation does NOT mean the module builds.
================================================================================

--------------------------------------------------------------------------------
10. SESSION LOG + LEARNINGS  (session 3, 2026-06-03)
--------------------------------------------------------------------------------
WHAT WAS DONE: every remaining actionable todo (P1-1, P1-2, P1-4, P2-1, P2-2, P3, P4, P5)
plus the P2-4 closed-decision write-up. Built with a phased, import-graph-aware parallel
workflow then app-layer wiring by hand. Module fully green: `go build ./...`, `go vet ./...`,
`gofmt -l .` (empty), `go test -race ./...` all pass; the binary was smoke-tested
(boot -> /healthz 200 -> tailscale2otel.up=1 + build_info on stdout -> clean SIGINT exit 0).

NEW THIS SESSION:
  - internal/collector/nodemetrics: gated node /metrics scraper (label-based instance, single
    MeterProvider, counters->deltas w/ reset handling, gauges, per-target tailscale.node.up,
    stdlib Prometheus text parser). config: collectors.node_metrics.
  - audit + flowlog processors: optional shared dedup.Set (cross-source dedup); app wires one
    set per processor. flowlog also adds tailscale.node.hostname to flow logs (P5).
  - tsapi.ConfigureLogStream (PUT) + putJSON; app App.autoConfigureStreaming (gated, off by
    default). Fixed a latent retry-transport bug: bodies are now rewound via GetBody on retry.
  - deploy/grafana/*.json (4 dashboards) + deploy/grafana/README.md; docs/metrics.md.
  - config: streaming.public_url; config.example.yaml refreshed (60s, rate_limit, admin,
    node_metrics) + a test that loads/validates the example to stop drift.
  - P2-1: slices.Contains, recorder .String() (deprecated .Emit() replacement).

ORCHESTRATION NOTE: ran the leaf Go work (audit/flowlog/nodemetrics/tsapi) + the no-Go
deliverables (dashboards/docs) as ONE parallel workflow, partitioned so concurrent agents
edited import-INDEPENDENT packages and built only their own package (tsapi gated to start
after audit+flowlog, which it imports). The shared test recorder (.Emit->.String) and the
config slices.Contains were done solo FIRST (recorder is a test dep of ~every package).

CLOSED DECISIONS (do not reopen without new reason): P2-4 per-device Resource = deferred (see
P2-4). P3 node identity = labels, not per-node Resource. P2-6 = blocked by pinned log SDK.
================================================================================
