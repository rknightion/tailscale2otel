================================================================================
tailscale2otel — HANDOFF / CONTEXT / TODO  (for resuming with an LLM)
================================================================================
Last updated: 2026-06-03 (session 4 EXECUTION). Single source of truth.

HOW TO READ THIS FILE:
  • ACTIVE (sections 1–8): what a new session needs NOW — status, hard
    constraints, agreed design decisions, the current package/telemetry maps,
    the LIVE backlog (section 7), and how to work here. READ THIS HALF before
    changing code.
  • HISTORICAL REFERENCE (sections H1–H6, BOTTOM): completed work (one-liners),
    the per-session execution logs, and the session-4 research evidence. Context
    only — consult for the "why" behind a finished item or the research backing a
    pending one. NOT active work.
Original design plan: /Users/rob/.claude/plans/we-are-building-a-cosmic-star.md


################################################################################
#  ACTIVE                                                                       #
################################################################################

--------------------------------------------------------------------------------
1. WHAT THIS PROJECT IS
--------------------------------------------------------------------------------
A lightweight Go service that polls the central Tailscale API
(api.tailscale.com/api/v2) for every available kind of observability data and
exports it as OTEL-native metrics + logs over OTLP. Optimized for Grafana Cloud
but works with any OTEL backend. Reference inspiration: ktranslate
(/Users/rob/repos/ktranslate) — sflow→OTEL; same pipeline shape on the TS API.

--------------------------------------------------------------------------------
2. CURRENT STATUS
--------------------------------------------------------------------------------
Feature-complete and GREEN. ALL of P0–P6 (sessions 1–3) and S4-1…S4-8 (session 4
execution) are DONE, committed, and PUSHED to origin/main (merge e1e8d59).
`go build ./...`, `go vet ./...`, `gofmt -l .` (empty), `go test -race ./...` all
pass; `helm lint`/`helm template` render; tools/configcheck builds+runs.
Verified END-TO-END against the real example.com lab + Grafana Cloud in session 2.

WHAT REMAINS (see section 7 for detail):
  • S4-9  LIVE re-verify of the session-3/4 additions in Grafana Cloud (incl. the
          S4-1 native-EventName Loki label + the audit dashboard/event_name
          migration). Needs example lab creds + a user go-ahead.
  • S4-10 LIVE stream-envelope capture (lab-authorized) → tighten the parser.
  • S4-11 Webhook enrichment (severity/data-field/dedup-map correctness).

--------------------------------------------------------------------------------
3. HARD CONSTRAINTS (evergreen — always read)
--------------------------------------------------------------------------------
3a. BUILD / TEST / METHODOLOGY
  - Module path: github.com/rknightion/tailscale2otel (also the OTEL
    instrumentation scope name — keep in sync).
  - Go: go.mod declares `go 1.26.0`; toolchain go1.26.x. Use 1.26 features freely
    (e.g. testing/synctest, used in internal/app/heartbeat_test.go).
  - After EVERY unit: go build ./... && go vet ./... && gofmt -l . (must be empty)
    && go test -race ./...   Commit a GREEN state between phases.
  - METHODOLOGY IS TDD (non-negotiable): failing test → watch it fail for the
    right reason → minimal code → green → refactor. stdlib `testing` ONLY (no
    testify). Assert emitted telemetry via internal/telemetrytest.Recorder (an
    in-memory OTEL reader) in every collector/processor test.

3b. PINNED DEPS (do NOT casually `go get`/`go mod tidy`)
    go.opentelemetry.io/otel v1.44.0 (+ /sdk, /sdk/metric, /metric, /trace,
      exporters otlpmetric{grpc,http}, stdout/stdoutmetric)
    go.opentelemetry.io/otel/log v0.20.0 (+ /sdk/log, exporters otlplog{grpc,http},
      stdout/stdoutlog)  [bumped from v0.6.0 in S4-1; has native Record.EventName]
    github.com/tailscale/tailscale-client-go/v2 v2.0.0-20250129222324-74c8fc3cb4d7 (pkg `tsclient`)
    golang.org/x/oauth2, gopkg.in/yaml.v3, github.com/klauspost/compress (zstd),
    github.com/tailscale/hujson, google.golang.org/grpc v1.81.1.
  Renovate groups ALL go.opentelemetry.io/otel{,/**} into ONE lockstep PR (core
  v1.x + the 0.x log modules MUST move together or the build breaks). tools/
  configcheck is a SEPARATE module (own go.mod + replace ../..) so `go build ./...`
  never touches it; it is NOT a runtime dep.

3c. tsclient/v2 QUIRKS (verify with `go doc` before assuming otherwise)
  - tsclient.Device is FLAT, NO ClientConnectivity field. The richer data (online
    flag, per-DERP latency, advertised/enabled routes, distro/os.version, nodeId)
    comes from the raw REST `GET /devices?fields=all` via tsapi.DevicesRich(), NOT
    from tsclient.Device. The devices collector uses DevicesRich.
  - Device field is `ID` (not NodeID) → DeviceMeta.NodeID + host.id. NO OSVersion
    on tsclient.Device. Timestamps are value-type `tsclient.Time` (embeds
    time.Time; guard .IsZero(), read .Unix()).
  - device.online is derived from LastSeen recency (default 5m window, injectable
    clock via devices/export_test.go SetClock).
  - tsclient.Key has NO KeyType → "type" derived from Capabilities
    (ephemeral|reusable|onetime). Key HAS Revoked (time.Time) + Invalid (bool).
  - User.LastSeen + Key.Expires are plain time.Time. User.Role/Status/Type are
    defined string types (string()).
  - TailnetSettings bools that EXIST: DevicesApprovalOn, DevicesAutoUpdatesOn,
    UsersApprovalOn, NetworkFlowLoggingOn, RegionalRoutingOn,
    PostureIdentityCollectionOn (+ DevicesKeyDurationDays int). NOT present:
    HTTPSEnabled, ACLsExternallyManagedOn.
  - RawACL has HuJSON (string) + ETag (string). DNSPreferences has MagicDNS (bool)
    only — NO OverrideLocal.
  - telemetrytest.Recorder renders int64 LOG attributes as "" (Value.AsString on
    Int64 kind). They emit correctly in prod; tests assert presence + verify the
    value via the human-readable log Body.

3d. ARCHITECTURE PRINCIPLES
  - Small single-purpose packages. Collectors depend only on the frozen contracts
    (telemetry.Emitter, collector interfaces, enrich.DeviceCache, tsapi.Client, the
    flowlog/audit processors). Each collector declares a NARROW client interface it
    needs (satisfied by *tsapi.Client) and fakes it in tests. The telemetry.Emitter
    facade is the ONLY thing touching OTLP, so OTLP never leaks into collectors.

--------------------------------------------------------------------------------
4. AGREED DESIGN DECISIONS (do not relitigate without reason)
--------------------------------------------------------------------------------
USER decisions: Go single static binary; flow logs as BOTH aggregated metrics AND
  full-fidelity OTEL logs; OTLP push (gRPC/HTTP) + stdout for local debug; scope =
  ALL Tailscale data sources; webhook receiver built but OFF by default; flow-log
  default verbosity per_connection (configurable per_record|off); BOTH poll AND
  stream (Splunk-HEC-style receiver) ingestion (S3/GCS object-reader DEFERRED).

ENGINEERING decisions:
  D1 Hybrid client: official tsclient for snapshots + a thin custom HTTP doer
     (tsapi/logs.go) for the two /logging endpoints. One shared retrying client.
  D2 Official OTEL log SDK (not ktranslate's third-party logs lib).
  D3 Flow-log "tail" hazard: never poll up to now; poll [checkpoint, now-lag].
     nextWindow() (collector/window.go) does cold-start lookback, lag, max-window cap.
  D4 Device identity = METRIC ATTRIBUTES (host.name/host.id/os.type/os.version/
     tailscale.user), NOT a per-device Resource. (See P2-4 closed decision.)
  D5 Synchronous instruments (Float64Counter.Add / Float64Gauge.Record).
  D7 Checkpoint store: in-memory default + file-backed opt-in (atomic temp+rename).
  D8 Streaming ~= polling in content; receiver emulates Splunk HEC (POST
     /services/collector/event, `Authorization: Splunk <token>`). TS does NOT
     document the exact HEC envelope → parser is DEFENSIVE; envelope still to be
     pinned by a live capture (S4-10).
  D9 [S4-1] Event name uses the NATIVE log.Record.EventName (log v0.20.0+), not the
     old "event.name" attribute. (Loki label re-verify pending — S4-9.)
  D10 [S4-4] Cumulative temporality pinned explicitly on both OTLP metric exporters
     (Grafana Cloud/Mimir accept cumulative ONLY; delta→HTTP 400). KEEP histogram
     _bucket/_sum/_count flattening (matches Mimir's classic storage; do NOT
     reconstruct native/exponential histograms — would lose explicit buckets).
  D11 [S4-3] webhook↔audit cross-dedup is BEST-EFFORT and OFF by default
     (webhook.dedup_audit_events). Shared dedup.Set, first-seen-wins; the
     type↔action map / subjectID / time-bucket need live tuning (S4-10/S4-11). It
     defaults to NO dedup whenever it can't confidently map (never over-suppresses).
  D12 [S4-7] Flow-log volume guard caps LOG records per window with a VISIBLE
     logs_dropped counter (no silent caps); metrics are NEVER capped. Stream body
     is bounded (decompressed) → 413 on overflow (anti-OOM/zip-bomb).
  - semconv naming: dotted lowercase, no `_total` suffix in source, UCUM units
    (By, {packet}, {flow}, {event}, {record}, s, d, 1). Low-cardinality dims on
    metrics; full 5-tuple / high cardinality only on log records.

CLOSED decisions (do NOT reopen without a new reason):
  - P2-4 PER-DEVICE RESOURCE = DEFERRED (user-confirmed). One Resource per
    MeterProvider, so true per-device Resource needs N providers + a target_info
    series per device = churn/cost for ~zero new capability (identity is already on
    every metric via D4). Node-level metrics are delivered by the nodemetrics
    scraper (label-based "instance"), not per-device Resource.
  - NODE-METRICS identity = the "instance" LABEL, NOT a per-node Resource.
  - P2-6 (no native EventName) = RESOLVED by S4-1 (D9).

--------------------------------------------------------------------------------
5. PACKAGE INVENTORY (all under internal/ unless noted) + PUBLIC CONTRACTS
--------------------------------------------------------------------------------
cmd/tailscale2otel/main.go - flags (-config), config.Load, slog, signals, app.New/Run.
internal/config       - Config + nested structs (yaml tags), Load(path), Default(),
                        (*Config).Validate(), Duration (.D()). ${ENV} expansion.
                        Defaults-merge = Default() then Unmarshal on top.
internal/telemetry    - Emitter{Counter,Gauge,UpDownCounter,LogEvent}; Attrs=map[string]any;
                        Event{Name,Body,Severity,Timestamp,Attrs}. NewEmitter(meter,logger).
                        Provider: NewProvider(ctx,Options) (grpc|http|stdout; cumulative
                        temporality pinned); .Emitter(); .Shutdown(). emitter.go uses
                        native Record.SetEventName.
internal/telemetrytest- Recorder.New(); .Emitter(); .MetricPoints(name)/.MetricNames();
                        .LogRecords() []{Body,SeverityText,EventName,Severity,Attrs}.
                        EventName reads the native field. TEST HELPER (no testing import).
internal/semconv      - shared attribute keys, UCUM units, enum values.
internal/dedup        - Set: bounded FIFO thread-safe de-dup set. New(cap); Add(key) bool.
internal/enrich       - DeviceCache: Replace([]DeviceMeta); ResolveName/LookupAddr/
                        LookupNode; Age()/Len()/WithClock(). Knows TS ranges.
internal/collector    - Collector/SnapshotCollector/WindowCollector ifaces; Registry;
                        CheckpointStore (Memory/File); Scheduler (goroutine+ticker per
                        collector, jitter, panic isolation, window cursor).
internal/flowlog      - record.go (NetworkResponse/FlowLog/ConnectionCounts);
                        processor.go NewProcessor(cache,Options{LogMode,IncludePorts,
                        NodeDims,KeepExternalAddrs,Dedup,MaxLogRecordsPerWindow}).
                        Consts MetricIO/Packets/Flows/LogsDropped. Adds node.hostname
                        via cache.LookupNode.
internal/audit        - record.go (ConfigurationResponse/Event/Actor/Target);
                        processor.go NewProcessor(opts...): WithDedup (eventGroupID-keyed,
                        poll↔stream) + WithCrossDedup (webhook cross-source). DedupKey +
                        NormalizedCrossKey/CrossSourceKey are the key-format homes.
internal/tsapi        - NewClient(Options); Devices/DevicesRich/Users/Keys/TailnetSettings/
                        Webhooks/PolicyFileRaw/DNS*; log doers NetworkFlowLogs/ConfigAuditLogs;
                        ConfigureLogStream (PUT). auth.go, transport.go (retry+rate-limit+
                        Bearer; GetBody rewind), logs.go.
internal/collector/{devices,users,keys,settings,acl,dns,flowlogs,auditlogs,nodemetrics}
                      - per-source collectors. nodemetrics: gated Prometheus-text scraper,
                        Target{URL,Instance,Labels,BearerToken,BearerTokenFile,Headers,TLS},
                        per-target *http.Client, counters→deltas, gauges, per-target node.up.
internal/stream       - New(Options{Listen,Path,Token,Decompress,TLS*,MaxBodyBytes},flowProc,
                        auditProc,emitter,logger); Handler/Run. HEC emulation, gzip/zstd,
                        defensive parse, body cap → 413.
internal/webhook      - New(Options{Listen,Path,Secret,Tolerance},emitter,logger,opts...);
                        WithDedup (cross-source). HMAC: header "t=<unix>,v1=<hex>", signed
                        "<t>.<body>", SHA256, constant-time. Empty Secret = SKIP verify.
internal/app          - options.go (config→telemetry/tsapi/nodemetrics opts); app.go
                        (New/Run, shared flowProc/auditProc + dedup sets, gated webhook
                        cross-set, graceful shutdown); collectors.go (registerCollectors +
                        buildReceivers); admin.go (/healthz,/readyz); heartbeat.go; selfobs.go.
Packaging/ops: deploy/Dockerfile, deploy/helm/tailscale2otel (full config in values.yaml,
  unconditional config helper, values.schema.json draft-07), deploy/docker-compose.yaml,
  deploy/grafana/*.json (4 dashboards)+README, deploy/alerts/*.yaml (9 rules),
  docs/metrics.md + docs/node-metrics.md, tools/configcheck (CI), .github/workflows/{ci,
  release,helm}.yml, config.example.yaml.

--------------------------------------------------------------------------------
6. AS-BUILT TELEMETRY CATALOG (current; OTLP→Prom normalization in docs/metrics.md)
--------------------------------------------------------------------------------
SELF-OBS (gated on self_observability.enabled):
  tailscale2otel.up[1] (heartbeat); .build_info[1]{service.version,go.version};
  .scrape.{duration[s],success[1],errors[1]{error.type},last_timestamp[s]}{tailscale.collector};
  .api.{requests[1]{endpoint,http.response.status_code},retries[1]{endpoint}};
  .export.failures[1]{error.type}; .enrich.{cache_age[s],cache_size[1]}.
NETWORK / FLOW:
  tailscale.network.io[By], .packets[{packet}] {direction,transport,traffic_type,src/dst node,ports?};
  .flows[{flow}] {transport,traffic_type}; .flow.logs_dropped[{record}] (S4-7 volume guard);
  tailscale.config.audit.events[{event}] {audit.action,audit.origin}.
DEVICES: device.online[1], .last_seen[s], .key.expiry[s], .update_available[1]
  {host.name,host.id,os.type,tailscale.user}; .derp.latency[s] (gated collect_routes via
  DevicesRich); .routes.{advertised,enabled}[{route}] (gated); devices.count[1].
USERS: users.count[1]; user.devices[1]; user.connected[1]; user.last_seen[s];
  user_invites.count[1]{role,accepted}.
KEYS: key.expiry[s]{key.id/type/description}; keys.count[1]{type,revoked,invalid}.
SETTINGS: setting.enabled[1]{setting.name}; setting.devices_key_duration[d].
ACL: acl.last_changed[s]; acl.size[By]; acl.rules[1]{acl.section}.
DNS: dns.{nameservers,search_paths,split_zones}.count[1]; dns.magic_dns[1].
FEATURE: feature.enabled[1]{tailscale.feature} (e.g. flow logging on/off).
RECEIVERS: stream.records{type=flow|audit}; stream.rejected{reason=auth|unparsable|too_large};
  webhook.events{webhook.type}; webhook.rejected{reason}.
NODEMETRICS (gated): tailscale.node.up[1]{instance} + every scraped tailscaled_* series
  re-emitted VERBATIM (counters→deltas, gauges→gauges) keyed by the "instance" label.
LOG RECORDS (native EventName): tailscale.network.flow (per conn/record); tailscale.config.audit
  (WARN on error); tailscale.key.expiring (WARN); tailscale.webhook.<type> (WARN for
  Expir/Suspend/NeedsApproval/Deleted — see S4-11 gap); tailscale.device.posture (gated).

--------------------------------------------------------------------------------
7. PENDING TASKS (incl. partially completed — THE LIVE BACKLOG)
--------------------------------------------------------------------------------
Methodology reminder: TDD, stdlib testing only, verify the WHOLE module green +
commit between units. Research evidence for these is in H6. Live-ops SAFETY: lab
creds in .secrets/creds.local.env (`set -a; source .secrets/creds.local.env; set
+a`), gcx context example; only the example LAB may be reconfigured; auto_configure
must NEVER target a real/production tailnet; NEVER commit .secrets/ or .capture/.

S4-9 [P0 / LIVE] RE-VERIFY the session-3/4 additions end-to-end in Grafana Cloud
     (only session-2's original telemetry was verified live). Confirm LIVE:
     (1) flow log records carry tailscale.node.hostname;
     (2) cross-source dedup (no double-count when poll + stream both on);
     (3) the 4 dashboards render vs REAL series — ESPECIALLY OTLP-normalized metric
         names match (highest-risk); add the new metrics (flow.logs_dropped, alerts);
     (4) [S4-1 TAIL] the NATIVE EventName: re-verify what Loki label the native
         EventName produces (Grafana Explore = source-of-truth), then migrate
         deploy/grafana/tailscale-audit-events.json + deploy/grafana/README.md +
         docs/metrics.md `event_name` variable/queries accordingly. Then P2-6/D9 done.
     (5) auto_configure path (only if doing S4-10).
     gcx gotcha: `gcx logs query` needs BOTH --from AND --to. Defer the nodemetrics
     live check — scraping is disabled on the lab.

S4-10 [P0 / LIVE] STREAMING ENVELOPE PINNING (finishes D8/P1-2's deferred capture;
     lab-AUTHORIZED). Stand up the stream receiver with an HTTPS endpoint reachable
     over the tailnet (`tailscale cert` for the node's *.ts.net name), point the
     example network+configuration log streaming at it (manually, or exercise
     streaming.auto_configure against the LAB ONLY), capture a REAL Splunk-HEC POST
     body → .capture/ (gitignored) → a SANITIZED testdata fixture, and TIGHTEN the
     defensive parser in internal/stream (TDD). Also captures real webhook events to
     unblock S4-11(d) subjectID + S4-3 live tuning.

S4-11 [P1] WEBHOOK ENRICHMENT — severity, data-field pinning, dedup-map correctness.
     (Researched 2026-06-03; evidence H6.) Generic dispatch in webhook.go (one log +
     one counter per `type`) is SOUND and STAYS; this is severity + attrs + dedup
     fixes from the authoritative catalog https://tailscale.com/kb/1213/webhooks#events.
     VALUE: webhooks carry client-misconfig/health + Tailnet-Lock events with NO
     log-streaming equivalent.
     (a) WARN-HEURISTIC GAP: warnSubstrings {Expir,Suspend,NeedsApproval,Deleted}
         MISSES exitNodeIPForwardingNotEnabled, subnetIPForwardingNotEnabled (the most
         actionable health alerts → emit at INFO), nodeNeedsSignature, and deprecated
         nodeNeedsAuthorization. Promote to WARN; prefer an explicit warn-type SET over
         substring matching.
     (b) ADDITIVE-VALUE EVENTS (no stream equivalent): exitNodeIPForwardingNotEnabled,
         subnetIPForwardingNotEnabled, nodeNeedsSignature, nodeSigned. Add a deploy/alerts
         rule on a nonzero rate of the two misconfig events (counter already keyed by type).
     (c) DEDUP-MAP (internal/webhook/crossdedup.go webhookActionMap, speculative): verify
         `userSuspended`+`userDeleted` against a live subscription and REMOVE if invalid
         (not in the docs). ADD real overlapping types: policyUpdate ({update,policy|tailnet}),
         nodeNeedsApproval ({create|update,node}), userNeedsApproval ({update,user}); +
         deprecated aliases nodeAuthorized→nodeApproved, nodeNeedsAuthorization→nodeNeedsApproval.
     (d) subjectIDKeys: node key is `nodeID` (ok) but the USER key is `user` (login/email
         string), NOT userID/userId/loginName/email; deviceID/deviceId never appear. Fix to
         {nodeID, user}. Documented data fields: node*={nodeID,deviceName,managedBy,actor,url};
         nodeKey* adds {expiration}; user*={user,url,actor}; userRoleUpdated adds {oldRoles,
         newRoles}; policyUpdate={newPolicy,oldPolicy,url,actor}; test=null. The `actor` field
         matches the config-audit actor format — strongest cross-source JOIN key; evaluate it.
     (e) PAYLOAD TYPING: webhook event.Data is map[string]string, but policyUpdate
         (oldPolicy/newPolicy) + userRoleUpdated (oldRoles/newRoles) are NOT flat strings →
         may fail to unmarshal/drop. Confirm shape in the S4-10 capture.
     UNCONFIRMED → needs the S4-10 live capture: the two misconfig events' data payloads are
     undocumented. No billing/connect-disconnect/route-change webhooks exist (FRs only:
     tailscale/tailscale#15638, #11166). TDD via telemetrytest; pair with S4-10.

(The Helm CI baseline chore is DONE — deploy/helm README.md + values.schema.json were
regenerated with the real helm-docs + losisin tools; see H5.)

--------------------------------------------------------------------------------
8. HOW TO CONTINUE (workflow for the LLM)
--------------------------------------------------------------------------------
1. Read the ACTIVE half of this file + the plan at
   /Users/rob/.claude/plans/we-are-building-a-cosmic-star.md.
2. Pick a task from section 7. For anything touching telemetry, FIRST read the
   frozen contracts: internal/telemetry/types.go, internal/collector/collector.go,
   internal/enrich/devicecache.go, internal/telemetrytest/recorder.go,
   internal/semconv/attrs.go, and the relevant processor.
3. Confirm any tsclient field/method via `go doc <type>` before using it (section 3c).
4. TDD: failing test → watch fail → minimal impl → green → refactor.
5. Keep packages small; collectors take a narrow client interface + fake it; assert
   via telemetrytest.Recorder.
6. After each unit: go build ./... && go vet ./... && gofmt -l . && go test -race ./...
7. ORCHESTRATION (if swarming): phase by the IMPORT GRAPH. Do shared/foundational
   packages (telemetry, config, app, dedup, telemetrytest) + any go.mod change SOLO,
   THEN fan out import-INDEPENDENT leaves (each agent owns ONE package, builds only
   its own). Re-verify the WHOLE module yourself after every workflow — leaf agents
   passing in isolation does NOT mean the module builds.
8. Commit to main freely; PUSH only when the user confirms.


################################################################################
#  HISTORICAL REFERENCE — completed work + logs (context only; NOT active)      #
################################################################################

--------------------------------------------------------------------------------
H1. COMPLETED MILESTONES (M0–M8)
--------------------------------------------------------------------------------
[x] M0 skeleton + OTLP heartbeat   [x] M1 TS client + auth (OAuth/API key, retry)
[x] M2 collector/scheduler framework (registry, scheduler, checkpoints, window math)
[x] M3 devices collector + enrichment cache   [x] M4 flow logs POLL + flowlog.Processor
[x] M5 audit logs POLL + audit.Processor   [x] M6 snapshot collectors (users/keys/settings/acl/dns)
[x] M7 HTTP receivers: Splunk-HEC stream + HMAC webhook (off by default)
[x] M8 packaging: Dockerfile, config.example.yaml, README, .dockerignore, .gitignore
[x] self-obs, real-tailnet+Grafana Cloud verification, CI/CD, Helm chart, docker-compose.

--------------------------------------------------------------------------------
H2. COMPLETED BACKLOG (one-liners; full detail in the session logs H3–H5)
--------------------------------------------------------------------------------
Session 2 (P0 + early P1/P2):
  P0-1 self-obs scrape/api/export/build_info/enrich metrics (gated).
  P0-2 wired config: cache_ttl→cache_age/size; collect_routes INLINE via DevicesRich; routes/posture.
  P0-3 window-boundary dedup in both window collectors (internal/dedup bounded set).
  P0-4 real-tailnet end-to-end verify + flow self-disable (FeatureCheck/403, feature.enabled).
  P0-5 CI: .github/workflows/{ci,release}.yml (build/vet/gofmt/test-race/govulncheck + image).
  P1-3 user-invites + device routes (inline) + acl.rules (hujson-parsed).
  P1-5 app-level integration test (newApp seam + recorder + stub client).
  P1-6 global token-bucket rate limiter (tailscale.http.rate_limit; validated @15 req/s).
  P2-3 build_info metric + always-on admin server (/healthz,/readyz).
Session 3 (remaining P1/P2/P3/P4/P5):
  P1-1 deploy/grafana 4 dashboards (Grafana 13 dynamic) + README.
  P1-2 streaming auto_configure PUT (ConfigureLogStream) + app wiring; parser hardened +
       realistic fixtures. LIVE-capture pinning deferred → S4-10.
  P1-4 cross-component shared dedup.Set on flowlog+audit processors (poll↔stream). Webhook↔audit
       was best-effort/uncovered → done in S4-3.
  P2-1 modern-stdlib lint (slices.Contains, recorder .String(), maps.Copy, strings.SplitSeq).
  P2-2 docs/metrics.md (naming + OTLP→Prom normalization + tables + query examples).
  P3   internal/collector/nodemetrics gated scraper (label "instance" identity, counters→deltas
       with reset handling, gauges, per-target node.up, stdlib parser; off by default).
  P4   default otlp.metric_interval 30s→60s; config.example.yaml refreshed + load/validate test.
  P5   flowlog adds tailscale.node.hostname via cache.LookupNode(flow.NodeID).
Session 4 EXECUTION (S4-1…S4-8 — detail in H5):
  S4-1 OTEL log SDK v0.6.0→v0.20.0 + native Record.EventName (zero core churn). [Loki/dashboard
       event_name migration PENDING → S4-9.]
  S4-2 nodemetrics per-target auth/TLS (bearer/headers/tls) + docs/node-metrics.md.
  S4-3 webhook↔audit cross-dedup (best-effort, off by default; mechanism tested).
  S4-4 cumulative temporality pinned on both OTLP metric exporters.
  S4-5 renovate.json (OTel lockstep group; reconciled with the repo's Renovate PR #2).
  S4-6 Helm config automation (unconditional config helper, full config in values.yaml,
       values.schema.json, Chart 0.2.0, tools/configcheck, helm.yml CI).
  S4-7 flowlog MaxLogRecordsPerWindow cap + logs_dropped counter + stream MaxBodyBytes cap.
  S4-8 deploy/alerts/ (9 Prometheus rules, OTLP-normalized names).

--------------------------------------------------------------------------------
H3. SESSION 2 LOG + LEARNINGS (2026-06-02)
--------------------------------------------------------------------------------
Built all P0 + P1-3/5/6 + P2-2(partial)/P2-3 + P4 by a phased parallel-workflow swarm
(each agent ONE package; strict TDD), app-layer integration by hand. Commits: 701ce13,
859ba93, bbcfc42.

REAL-TAILNET TESTING: lab = example.com; read-only creds + Grafana Cloud OTLP creds in the
GITIGNORED .secrets/creds.local.env (`set -a; source .secrets/creds.local.env; set +a`).
NEVER commit .secrets/, .capture/, or config.{local,smoke,lowlog}.yaml. Real API captures
in .capture/ for fixtures. Tailnet: 89 devices, flow logging ON, posture ON.

4 PRODUCTION-BREAKING BUGS found ONLY against real data (synthetic fixtures missed them):
  1. flow-log `proto` is a NUMBER on the wire (1/6/17/99/absent), not a string → `Proto
     string` made json.Decode FAIL → flow polling silently emitted NOTHING. Fixed:
     ConnectionCounts.Proto int + transportName() IANA→name mapper.
  2. audit old/new are POLYMORPHIC (string|object|array|null) → `string` failed to decode.
     Fixed: json.RawMessage + renderRaw().
  3. OTLP/HTTP to Grafana Cloud 404'd: otlphttp WithEndpointURL uses the path AS-IS, does
     NOT append /v1/metrics. Fixed: otlpHTTPURL() appends /v1/{metrics,logs}.
  4. graceful SIGINT returned context.Canceled → nonzero exit. Fixed: app.Run treats a ctx
     error from the scheduler as a clean stop.

KEY WIN: raw `GET /devices?fields=all` (tsapi.DevicesRich) returns far richer data than
tsclient.Device — connectedToControl, per-DERP latency, advertised/enabled routes INLINE,
distro/os.version, nodeId (nDdi form, keys the cache byNode).

GRAFANA CLOUD (gcx context example, Grafana 13.1.0): metrics → Mimir (ds grafanacloud-prom);
logs → Loki (ds grafanacloud-logs, label service_name="tailscale2otel"). OTLP→Prom
normalization: dots→`_`, counters +`_total`, units suffix (By→_bytes, s→_seconds, d→_days),
UNIT "1" gauges +`_ratio` (e.g. tailscale_device_online_ratio, tailscale2otel_up_ratio).
gcx GOTCHA: `gcx logs query` needs BOTH --from AND --to; Grafana Explore is source-of-truth.

ORCHESTRATION LEARNINGS: phase by the IMPORT GRAPH — shared-dep packages (telemetry,
collector top-level, telemetrytest, config, semconv) editing CONCURRENTLY with leaf agents
causes transient compile races; sequence shared changes BEFORE the leaf fan-out. Type
changes ripple to CONSUMER test fixtures in OTHER packages. Always verify the WHOLE module +
commit a GREEN state between phases.

--------------------------------------------------------------------------------
H4. SESSION 3 LOG + LEARNINGS (2026-06-03)
--------------------------------------------------------------------------------
Did every remaining actionable todo (P1-1/1-2/1-4, P2-1/2-2, P3, P4, P5) + the P2-4 closed
decision. Phased import-graph-aware parallel workflow + app wiring by hand. Module green;
binary smoke-tested (boot → /healthz 200 → up=1 + build_info → clean SIGINT exit 0).
NEW: nodemetrics scraper; shared dedup.Set on audit+flowlog; flow node.hostname (P5);
tsapi.ConfigureLogStream PUT + autoConfigureStreaming (fixed a retry-transport GetBody-rewind
bug); 4 grafana dashboards + README; docs/metrics.md; streaming.public_url + example refresh.
CLOSED: P2-4 per-device Resource deferred; P3 node identity = labels; P2-6 unblocked → S4-1.

--------------------------------------------------------------------------------
H5. SESSION 4 LOG + LEARNINGS (2026-06-03: planning/research THEN execution)
--------------------------------------------------------------------------------
PLANNING (no code): a 5-agent web-research fan-out + local `go doc` probe scoped the S4
backlog with evidence in H6. Headline: P2-6 unblocked (log v0.20.0 has native EventName,
core stays v1.44.0).

EXECUTION: implemented S4-1…S4-8 with strict TDD, phased by the import graph, committing a
green state per phase. Commits on main (on cc00c3d), then merge e1e8d59 (origin Renovate PR):
  99aa13c Phase A SOLO: S4-1 native EventName + S4-4 temporality pin
  3e58419 Phase B leaves (parallel wf): S4-2 nodemetrics auth/TLS + S4-7 flowlog cap
  5caba8d S4-3 cross-dedup (solo) + S4-7 stream body cap (solo)
  f83dd57 docs: S4-11 backlog (user-prompted webhook research)
  9ac6e7b Phase C SOLO: config + app wiring
  db63b77 Phase D (parallel wf): S4-5 renovate + S4-6 helm + S4-8 alerts + S4-2 docs
  5da0b09 Review fixes: 5 adversarially-confirmed findings
ORCHESTRATION: shared/foundational work SOLO (Phase A telemetry+go.mod; Phase C config+app;
S4-3 spans dedup+audit+webhook). Import-independent leaves via parallel Workflows; re-verified
the WHOLE module after each. A 13-agent adversarial review (find → refute-by-default) gave 8
candidates; the 5 confirmed real were fixed (5da0b09): Helm empty-secret→HMAC-skip footgun
(documented + surfaced in values.yaml); a non-discriminating TLS test (rewritten as a 2-target
differentiator); a missing flow-cap test on the standalone Process() path; an orphaned
values.schema.json property (CI fail-on-diff risk); an undocumented logs_dropped metric.
LEARNINGS:
  - gopls/LSP lags a go.mod version bump (STALE "undefined method" diagnostics); the compiler
    (go build / go doc) is source-of-truth — verified the v0.20.0 native methods via go doc.
  - A build-tag-only Go package breaks `go build ./...`; the CI configcheck is a SEPARATE
    module (own go.mod + replace ../..) which `./...` naturally excludes.
  - webhook↔audit cross-dedup needs LIVE capture to actually be effective (no webhook captures
    exist; IDs likely differ across sources) — built+tested the mechanism, deferred tuning (S4-11).
  - renovate.json: the repo already had Renovate (PR #2); reconciled by keeping the user's
    config + ADDING the OTel lockstep group (build-break safety).
  - Helm baselines: installed helm-docs 1.14.2 (brew) + helm-values-schema-json v2.4.0 (go
    install — losisin's brew tap is blocked by a Workbrew git-credential quirk) and
    regenerated deploy/helm README.md + values.schema.json from the REAL tools so helm.yml's
    fail-on-diff CI is stable (the actions @v1/@v2 track the same tool majors).

--------------------------------------------------------------------------------
H6. SESSION-4 RESEARCH FINDINGS (evidence for S4-9/10/11; backs completed S4 items)
--------------------------------------------------------------------------------
H6.1 OTEL LOG SDK (S4-1, DONE). log v0.20.0 (2026-05-27) built from the SAME commit as core
  v1.44.0; pins otel v1.44.0/grpc v1.81.1/protobuf v1.36.11/proto-otlp v1.10.0 — all ours →
  zero core churn. `go doc` confirmed Record.EventName()/SetEventName(). No breaking changes
  affect us (the breakers are in the sdk/log Processor EXTENSION API we don't use).
  otlploggrpc/stdoutlog v0.12.0 are RETRACTED (broken logtest dep); v0.20.0 skips them.
  RISK (→ S4-9): anything querying the `event.name` LOG ATTRIBUTE in Loki must migrate to the
  native EventName (incl. tailscale-audit-events.json `event_name`).

H6.2 GRAFANA CLOUD TEMPORALITY + HISTOGRAMS (S4-4, DONE). Mimir/Grafana Cloud OTLP accept
  CUMULATIVE ONLY (delta → HTTP 400, mimir#6696; server-side delta is an open FR, mimir#10439).
  Explicit-bucket histograms stored as classic _bucket/_sum/_count (NHCB needs a distributor
  flag you can't toggle on hosted GC) → KEEP flattening; reconstructing native/exponential
  histograms would lose tailscaled's explicit buckets. Act only on cardinality cost.

H6.3 TAILSCALED NODE METRICS (S4-2, DONE). Client metrics since v1.78.0: `tailscale set
  --webclient` → Prometheus-text /metrics. Local http://100.100.100.100/metrics (quad-100);
  tailnet http://<tsIP>:5252/metrics. PLAIN HTTP, NO token/TLS — access = a tailnet ACL/grant
  opening port 5252 ({"grants":[{"src":["tag:monitoring"],"dst":["tag:server"],"ip":["5252"]}]}).
  CLI alts: `tailscale metrics print|write`. tailscaled --debug=localhost:8080 → /debug/metrics
  (localhost-only, unauthenticated, UNSTABLE superset — prefer client metrics). Documented
  families: counters tailscaled_{inbound,outbound}_{packets,bytes}_total, _dropped_packets_total,
  _peer_relay_forwarded_{packets,bytes}_total; gauges tailscaled_{advertised,approved}_routes,
  _peer_relay_endpoints, _health_messages, _home_derp_region_id. Throughput counters carry a
  `path` label (direct_ipv4/ipv6, derp, peer_relay_ipv4/ipv6) → cardinality = nodes×path×metric.
  Nodes < v1.78 → node.up=0. (Per-target auth/TLS fields built in S4-2.)

H6.4 RENOVATE (S4-5, DONE). config:best-practices; CRITICAL OTel lockstep group (core v1.x +
  0.x log modules in ONE PR or the build breaks); 0.x minors never automerged; osvVulnerability
  Alerts; minimumReleaseAge 3d. (The shipped renovate.json reconciles this with the repo's
  existing Renovate PR #2 config — see H5.) go 1.26 handled via go-version-file.

H6.5 HELM CONFIG AUTOMATION (S4-6, DONE). Helm validates draft-07 ONLY (helm#13069) → schema is
  draft-07. Generate values.schema.json with losisin/helm-values-schema-json (NOT
  invopop/jsonschema — emits 2020-12, reads json tags while our struct uses yaml tags). The
  generator + helm-docs are CI tools (a separate module, no runtime dep). Cross-field rules that
  draft-07 can't express stay in config.Load, exercised by tools/configcheck.

H6.6 TAILSCALE WEBHOOKS (S4-11, PENDING). Authoritative catalog
  https://tailscale.com/kb/1213/webhooks#events. Webhooks carry client-misconfig/health
  (exitNodeIPForwardingNotEnabled, subnetIPForwardingNotEnabled) + Tailnet-Lock (nodeNeedsSignature,
  nodeSigned) events with NO log-streaming equivalent. Documented data fields: node*={nodeID,
  deviceName,managedBy,actor,url}; nodeKey* adds {expiration}; user*={user,url,actor};
  userRoleUpdated adds {oldRoles,newRoles}; policyUpdate={newPolicy,oldPolicy,url,actor};
  test=null. The `actor` field matches the config-audit actor format (strongest cross-source
  join key). UNCONFIRMED: the two misconfig events' data payloads (no example) — need a live
  capture (S4-10). userSuspended/userDeleted are NOT in the docs (userDeleted only in a Pulumi
  sample) — verify vs a live subscription. No billing/connect-disconnect/route-change webhooks
  exist (FRs: tailscale/tailscale#15638, #11166). Full action items in section 7 (S4-11).
================================================================================
