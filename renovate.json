{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "vulnerabilityAlerts": {
    "automerge": false
  },
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "lockFileMaintenance": {
    "enabled": true,
    "automerge": false
  },
  "automergeType": "pr",
  "platformAutomerge": false,
  "packageRules": [
    {
      "description": "Never automerge release-please PRs - manual review required",
      "matchFileNames": [".release-please-*.json"],
      "automerge": false
    },
    {
      "description": "Label GitHub Actions for manual review",
      "matchManagers": ["github-actions"],
      "labels": ["dep:gha"],
      "automerge": false
    },
    {
      "description": "Label patch and minor updates for manual review",
      "matchUpdateTypes": ["patch", "minor"],
      "automerge": false,
      "labels": ["dep:low-risk"]
    },
    {
      "description": "Label pin updates for manual review",
      "matchUpdateTypes": ["pin"],
      "automerge": false,
      "labels": ["dep:low-risk"]
    },
    {
      "description": "Label digest updates (Docker, etc) for manual review",
      "matchUpdateTypes": ["digest"],
      "automerge": false,
      "labels": ["dep:low-risk"]
    },
    {
      "description": "OpenTelemetry: update core (v1.x) and the 0.x log modules together in ONE PR and never automerge - a core bump without the log modules (or vice-versa) breaks the build. This rule is last so it overrides the broad patch/minor automerge above for these packages.",
      "matchManagers": ["gomod"],
      "matchPackageNames": ["go.opentelemetry.io/otel{,/**}"],
      "groupName": "OpenTelemetry",
      "automerge": false
    },
    {
      "description": "Never automerge pre-1.0 (0.x) Go module minor/patch updates - a 0.x minor can be breaking (e.g. the otel log modules).",
      "matchManagers": ["gomod"],
      "matchCurrentVersion": "< 1.0.0",
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": false
    }
  ]
}
