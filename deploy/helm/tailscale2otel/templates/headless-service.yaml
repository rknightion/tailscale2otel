{{- if eq .Values.config.coordination.mode "kubernetes" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "tailscale2otel.fullname" . }}-headless
  labels:
    {{- include "tailscale2otel.labels" . | nindent 4 }}
spec:
  clusterIP: None
  selector:
    {{- include "tailscale2otel.selectorLabels" . | nindent 4 }}
{{- end }}
